# FocusLock v1.1.0 - Requirements
# Core app runs on standard library only (Python 3.10+)

# Optional - for system tray icon support
pystray>=0.19.0
Pillow>=10.0.0

# Optional - for building a standalone .exe
pyinstaller>=6.0
